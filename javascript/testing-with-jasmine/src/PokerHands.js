function PokerHands() {

}

/**
 * Compares the hands of two players.
 *
 * @param cardsOfPlayerOne
 * @param cardsOfPlayerTwo
 * @returns -1 if playerOne has the better hand, +1 if playerTwo has the better hand or zero if both hands are equal
 */
PokerHands.prototype.compare = function(cardsOfPlayerOne, cardsOfPlayerTwo) {
    return 0;
};

