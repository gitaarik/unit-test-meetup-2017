describe("Primefactors", function () {

    var primefactors;

    beforeEach(function () {
        primefactors = new Primefactors();
    });


    // it("should determine that one has not prime factors", function () {
    //     expect(primefactors.find(1)).toEqual([]);
    // });


});
